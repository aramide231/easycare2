import ReminderPage from "@frontdesk/pages/set-reminder/ReminderPage";

/** Diagnostics Set Reminder — same UI as Front Desk. */
const SetReminder = () => {
  return <ReminderPage />;
};

export default SetReminder;
